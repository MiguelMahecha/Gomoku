package domain;

public class GoldenTile extends Tile {
    /**
     * Create a new Tile
     *
     * @param row   The tile's row
     * @param col   The tile's column
     * @param board The board that the Tile belongs to
     */
    public GoldenTile(int row, int col, Board board) {
        super(row, col, board);
    }
}
